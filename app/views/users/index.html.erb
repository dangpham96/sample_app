<% provide :title, t("login-index-user.tittle-index-user1") %>
<h1><%= t("login-index-user.tittle-index-user1") %></h1>

<%= paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>
